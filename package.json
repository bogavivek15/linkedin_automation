{
  "name": "careeros",
  "version": "0.1.0",
  "private": true,
  "description": "CareerOS — Autonomous Multi-Agent Career Operating System",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start:api": "python -m uvicorn apps.api.app.main:app --port 8000 --reload",
    "test:api": "python -m pytest apps/api/tests"
  }
}
